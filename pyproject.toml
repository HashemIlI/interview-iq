[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "interview-iq"
version = "0.1.0"
description = "Interview IQ - NLP Module (Answer Correctness Evaluation)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]