# decisions.md — Interview IQ / السجل الموحّد للقرارات (D1–D41)
**الإصدار:** v2.3 — 9 يوليو 2026
**الحالة:** السجل القانوني الوحيد بعد حسم Q1 (يدمج decisions.md القديم + DECISIONS_RECONCILIATION_v1.1 ويُلغيهما كملفين منفصلين)
**القاعدة الحاكمة:** أرقام D1–D25 ثابتة كما في الوثيقة الرئيسية `InterviewIQ_Pipeline_Docs_v2.0.md`. قرارات جلسة الـ pipeline أُعيد ترقيمها D26–D34. أي قرار جديد يأخذ الرقم التالي (D42+).

**مفتاح الحالة:** ✅ مقفول ونهائي · ⛔ مفتوح/معلّق · 🔶 empirical/غير مُتحقق منه · 🗑️ ملغي

---

## القسم الأول — D1–D25 (السجل القانوني في الوثيقة الرئيسية)

النصوص الكاملة لهذه القرارات موجودة في `InterviewIQ_Pipeline_Docs_v2.0.md` ولا تُنسخ هنا. تُذكر أدناه فقط البنود المُشار إليها في هذا الملف أو المعدَّلة بالتوحيد:

- **D19 ✅** — التطوير محلي (Claude Code + Git)، GitHub = المصدر الوحيد للحقيقة، Kaggle محرك GPU عبر Thin Runners.
  - *انحراف موثق (8 يوليو) — مُغلق:* نوتبوك `kaggle_runner_lora.ipynb` المرحلي كان يسحب السكريبتات من Kaggle Dataset بدل git clone لأن الريبو لم يكن موجودًا. انتهى بنشر الريبو.
  - *Q9 مُغلقة (9 يوليو 2026):* الريبو الرسمي = https://github.com/HashemIlI/interview-iq (private). نوتبوكات الـ Runners تسحب منه حصريًا عبر `git clone` + PAT من Kaggle Secrets (`GH_TOKEN`).
  - *ملاحظة بيئة (9 يوليو 2026):* صورة Kaggle الافتراضية تحوي `peft` أحدث + `torchao==0.10.0` غير متوافقة ⇒ فشل `get_peft_model` بـ `ImportError`. الـ runner يفرض نسخ `requirements.txt` صراحةً ويزيل `torchao` (غير مستخدم — لا تكميم في التصميم).
- **D20 ✅ / 🗑️** — Linguistic Confidence Module خارج النطاق نهائيًا؛ كل ما يخصه ملغي.
- **D21 ✅** — Option B Dual-Channel Scoring (Precision + Coverage → Harmonic F). *(ملاحظة توحيد: "D21 تحديث" في السجل القديم كان قرارًا مختلفًا — أصبح D26 أدناه.)*
- **D22 ✅ (مُعدَّل بالتوحيد)** — يُضاف إليه رسميًا: **τ_E = 0.9** بوسم `PRE-CALIBRATION DEFAULT — NOT VALIDATED`.
- **D24 ✅** — Baseline Demo للمشرف. *(تصادم الترقيم مع "P09 Edge Case" حُسم: الأخير أصبح D29.)*
- **D25 ✅ (مُقيَّد بالتوحيد)** — طبقات البيانات الثلاث (Easy Pos/Neg/Hard Neg) صُممت قبل تشخيص الـ Subject Blindness؛ **بنية بيانات الـ NLI المنفَّذة فعليًا هي تصنيفات D32 الستة وتحل محلها**. تبقى سارية من D25: عتبات الـ zero-shot المسجّلة مسبقًا (Contradiction F1 < 0.75 **أو** E↔C confusion > 5% ⇒ fine-tuning إلزامي).
  - **تدقيق لاحق (D41 — 9 يوليو 2026):** على الـ Gold Set، الـ zero-shot سجّل `Contradiction F1 = 0.865` ⇒ **الشرط الأول لم يتحقق**. المُفعِّل الفعلي هو الشرط الثاني وحده: `E↔C = (3+1)/48 = 8.3% > 5%`. أي وصف بأن الـ zero-shot "فشل فشلًا ذريعًا" **غير مدعوم بالـ Gold Set** ولا يُكتب في الرسالة. Demo #1 كان قراءة احتمالات خام على 5 أسئلة، لا argmax على 48 زوجًا — سياقان مختلفان.

---

## القسم الثاني — D26–D41

## D26 — Hard Positive Pairing Rule (✅ مقفول) *(سابقًا: "D21 تحديث")*
- كل HARD_POS instance يُصدر **زوجين إجباريين مرتبطين بنفس claim نصيًا**:
  - Pair A: premise = chunk الأصلي (كيان X)، label = entailment
  - Pair B: premise = chunk عن كيان آخر Y في نفس الوثيقة (distractor)، نفس الـ hypothesis، label = **neutral** (وليس contradiction — الـ claim صحيح تقنيًا، الـ premise B فقط لا يثبته لأنه عن موضوع مختلف)
- تسمية: الـ N الناتج = `paired_neutral` (مرتبط بـ claim_id)، مميز عن `standard_neutral` العادي.
- **قاعدة تشغيلية:** الـ twins لا يُفصلان أبدًا عبر حدود train/val.
- *تحقق آلي (9 يوليو 2026):* `validate_data` أكد **30 مجموعة twin سليمة** في الـ 150 زوجًا.
- **أثر تجريبي مرصود (D41):** الـ twins = 60/150 زوجًا = **40% من إشارة التدريب**، وكلها تُعلّم صراحةً: "premise عن كيان مختلف ⇒ neutral". هذا التصميم هو **التفسير الميكانيكي** لانزياح الموديل المدرَّب نحو `neutral` بعد الـ fine-tuning. التصميم يعمل كما قُصد؛ الأثر الجانبي لم يكن مسجَّلًا مسبقًا.

## D27 — Pilot Scope Reduction (🔶 empirical, capacity-driven) *(سابقًا: D22)*
- سبب القرار: مراجعة 47 وثيقة قبل بدء التوليد غير واقعية ضمن ميزانية المراجعة الفعلية المتاحة (8h مخصصة أصلًا لـ Stage 4 لمراجعة NLI pairs، وليس لمراجعة الوثائق نفسها — بند O13 كان منفصلًا وغير محسوب).
- القرار: Phase 1 pilot = **10 وثائق فقط (2 لكل track)**، مختارة لأنها **contrastive** (تحتوي على كيانين متقابلين على الأقل) لتفادي مشكلة Stage 0 Triage بالكامل.
- الوثائق المعتمدة (✅ روجعت يدويًا): DA-001, DA-003, DS-001, DS-003, CS-002, CS-004, SE-001, SE-005, GN-001, GN-008.
- توسيع الـ pool للـ 47 (أو أكتر) مؤجل لما يثبت الـ pipeline نجاحه end-to-end على الـ pilot ده، وبعد تخصيص ميزانية وقت منفصلة لمراجعة الوثائق تحديدًا.

## D28 — Gold Set Leakage Resolution (✅ confirmed) *(سابقًا: D23)*
- الـ 48-pair Gold Set بالكامل مصدرها **سؤال واحد فقط: DS-014 (Overfitting)**، مش 48 سؤال مختلف.
- القاعدة: **DS-014 مستبعد نهائيًا من premise pool أي بيانات تدريب**، في كل الـ Phases القادمة، بلا استثناء. يبقى محجوز حصريًا كـ held-out evaluation set.
- التطبيق الآلي: فحص إلزامي في `validate_data` (Phase 3) + إعادة تأكيد داخل `finetune.py` وقت التدريب (`D28ContaminationError`) + **بوابة على مستوى نظام الملفات** في runner الـ Kaggle (انظر D37).
- *تحقق (9 يوليو 2026):* صفر تلوث في الـ 150 زوجًا.

## D29 — P09 Edge Case Resolution (✅ محسوم) *(سابقًا: D24 في السجل القديم — أُعيد ترقيمه لفض التصادم مع D24 القانوني)*
- الحالة: DA001-P09 (subject-swap) — "شبه المهيكلة ليس لها أي شكل تنظيمي" مقابل premise C03 "تحتوي على وسوم/مفاتيح تنظيمية".
- القرار: **contradiction مؤكد**، وليس weak contradiction. السبب: الادعاء ينفي وجود أي تنظيم بالمطلق، بينما الـ premise يثبت وجود تنظيم جزئي (وسوم) — هذا تناقض منطقي مباشر (النفي المطلق يصطدم بإثبات جزئي)، وليس بديلين غير حصريين كحالة weak contradiction المرجعية من الـ Gold Set (**P48: طريقتا كشف غير متعارضتين**). يُستخدم هذا المثال كـ calibration reference لتمييز الحالتين مستقبلًا (مع DA003-P10 — انظر D31).
- **ملاحظة لاحقة (D41):** توصيف P48 أعلاه — المكتوب **قبل** تشغيل Phase 5 — يتسق مع تصنيف الموديل المدرَّب له كـ `neutral`، رغم أن الليبل الذهبي `contradiction`. **الليبل لم يُعدَّل.** انظر D41.

## D30 — Pilot Generation Complete (✅ 150/150 pairs، Stages 1–3) *(سابقًا: D25 في السجل القديم)*
- الـ 10 وثائق كلها اكتملت (Stage 1 Generator → Stage 2 blind Reviewer → Stage 3 QA Audit البرمجي والنوعي). التوزيع الكلي: E=50 (33.3%) | C=60 (40%) | N=40 (26.7%). مطابق للهدف 35:40:25 بفارق ≤2 نقطة على مستوى العينة الكاملة.
- Subject-swap = 40/60 من فئة C = **66.7%** (دون الـ 73% المستهدف — الـ 73% كان الهدف المتفق عليه، والفارق ثابت عبر كل الوثائق ويُعوَّض عند التوسع لبقية الـ 37 وثيقة).
- *تحقق آلي (9 يوليو 2026):* `validate_data` أكد التوزيع E=50/C=60/N=40 بالضبط.

## D31 — Genuine Stage 2 Pass on 9-Doc Set (✅ completed, findings logged) *(سابقًا: D26 في السجل القديم)*
- تصحيح صريح: التأكيد السابق على "Stage 3 برمجي ✅" في qa_audit_DA001.md كان افتراضًا وليس تنفيذًا فعليًا (لا توجد أداة embedding-similarity حقيقية في تلك الجلسة). المراجعة اليدوية الفعلية كشفت ما كان يُفترض أن يُكتشف آليًا.
- **نتيجة:** 132/135 confirmed، **2 near-duplicates حقيقية** تم استبدالهما (DS003-P06, CS004-P06 — نفس pair_id/category/label/chunk_id، هيبوثيسيس جديد بس)، و**1 حالة ثقة أقل** موثقة (DA003-P10، مرجع معايرة إضافي مع DA001-P09/D29).
- الملف المعتمد: `pairs_pilot_remaining9_v2.json` — **يُلغي** الاعتماد على v1. العدد والتوزيع الكلي (135، 54C/45E/36N) لم يتغيرا.
- **درس مسجل (قاعدة عامة):** أي ادعاء "تم الفحص البرمجي" لازم يُفرَّق فيه بين "نُفِّذ فعليًا بأداة" و"افتُرِض/قُيِّم يدويًا/استُنتج من ملف config". الخلط بين الاثنين أدى لتفويت duplicate حقيقي (هذا البند)، ولاحقًا لادعاءين خاطئين عن نطاق الـ fine-tuning (D38) — كلاهما نُقض بالتشغيل التجريبي. **القاعدة تُرفع إلى §5 من `PROJECT_EXECUTION_PLAN.md` (Rule 13).**

## D32 — Per-Document Quota (✅ مقفول) *(كان بلا رقم في السجل القديم)*
- يحقق التوزيع 33.3:40:26.7 ≈ الهدف 35:40:25. **يحل محل طبقات D25 الثلاث في بنية بيانات الـ NLI** (انظر القسم الأول).

| Category | n instances | E | C | N |
|---|---|---|---|---|
| EASY_POS | 2 | 2 | | |
| HARD_POS | 3 | 3 (A) | | 3 (B) |
| EASY_NEG | 1 | | 1 | |
| SUBJECT_SWAP | 4 | | 4 | |
| HN_OTHER | 1 | | 1 | |
| STANDARD_NEUTRAL | 1 | | | 1 |
| **إجمالي/وثيقة** | **12 instances → 15 pairs** | **5** | **6** | **4** |

- Subject-swap share من C = 4/6 = 66.7% (انظر D30 لخطة التعويض عند التوسع).
- حجم الـ Pilot: 10 وثائق × 15 pair = **150 pair**.

## D33 — ⚠️ RISK ACCEPTED: Stage 4 غير منفَّذة — بوابة G1 مغلقة بقبول المخاطرة (✅ مقفول بقرار)
- بضغط الوقت (سماحية الدكتور)، اتقفلت مراجعة الوثائق العشرة نفسها (المحتوى المرجعي)، لكن **مراجعة الـ 150 pair الفردية (Stage 4) غير منفَّذة** ما عدا P09 اللي اتحسم بمنطق موثق (D29).
- هذا انحراف موثق عن المنهجية المقفولة (Stage 4 = non-negotiable بشري)، متخذ بقرار أحمد الصريح.
- **القرار النهائي (9 يوليو 2026):** تُستكمل Phases 5+ **دون** إجراء الـ 20% retroactive spot-check. البوابة **G1 مغلقة بقرار قبول المخاطرة، لا بإتمام المراجعة.**
- **الالتزام المقابل (غير قابل للتنازل):** يُذكر في الرسالة وفي المناقشة صراحةً:
  > "لم تخضع الـ 150 زوجًا لمراجعة بشرية per-pair (Stage 4). خضعت للمراجعة الآلية والنوعية (Stages 1–3) ولمراجعة الوثائق المرجعية العشرة يدويًا. هذا حدٌّ معروف في صحة الـ labels، ونتائج الـ fine-tuning تُقرأ في ضوئه."
- **تخفيفات قائمة:** (أ) الـ Gold Set (48 زوجًا، DS-014) مستقل تمامًا عن بيانات التدريب؛ (ب) D31 وثّق اكتشاف duplicates حقيقية عند المراجعة اليدوية الفعلية ⇒ جودة الـ pipeline التوليدي مُقيَّمة جزئيًا؛ (ج) أي أثر لـ labels فاسدة يظهر في confusion matrix الـ Gold Set في Phase 5.
- كل نتيجة fine-tuning تُنتَج تحت هذا القرار توسم `RISK ACCEPTED` في اللوجات والتوثيق.

## D34 — تأجيل أزواج اتجاه الـ Coverage (✅) *(قرار مُتخذ في جلسة تصميم الـ pipeline ولم يكن مسجلًا — يُسجَّل الآن)*
- أزواج اتجاه الـ Coverage (key points كـ hypothesis) **مستبعدة من دفعة الـ fine-tuning الأولى** لتفادي تلويث نتائج الـ ablation الخاصة بعلاج الـ Subject Blindness.
- مرتبط بالقضية المؤجلة صراحةً (Coverage Asymmetry). **بانتهاء Phase 5 (D41) رُفع التأجيل — Q8 مفتوحة للنقاش الآن.**

## D35 — قبول مخالفة قاعدة الـ 5-word overlap (✅ مُصدَّق — 9 يوليو 2026، مُصحَّح مرتين)
- **الأرقام المعتمدة:** 65/150 زوجًا (43.3%). التوزيع: **Contradiction = 37 · Entailment = 28 · Neutral = 0**.
- **المصدر:** `scripts/recount_overlap.py` — خوارزميتان مستقلتان (5-gram set intersection + DP longest-common-substring) تعطيان نفس الناتج.
- **تعريف القياس:** NFC → حذف علامات Unicode فئة `Mn` (التشكيل) → lowercase → `re.findall(r'\w+', re.UNICODE)`، ثم ≥5 tokens متتالية مشتركة بين premise وhypothesis.
  **لا يشمل** توحيد رسم الألف/الهمزة/التاء المربوطة ⇒ الرقم 65 **حد أدنى (lower bound)**، لا عدّة نهائية.
- **تصحيح توثيقي:** نسخة سابقة من هذا القرار سجّلت "43/150 بتوزيع C=25/E=18". الرقم مُلغى. **سبب الاختلاف بين العدّتين لم يُحقَّق فيه، ولا يُفترض له تفسير في هذا السجل.**
- **القرار: قبول مع التوثيق، بلا إعادة كتابة.** المبرر:
  - *على محور E↔C:* التوزيع (37 C / 28 E) لا يُنشئ heuristic "overlap عالٍ ⇒ Entailment"، بل يعاكسه — وهو الاتجاه الضار الموصوف في McCoy et al. (2019 — HANS). الميل ناحية Contradiction مقصود بالتصميم: فئة subject-swap تتقاسم المعجم عمدًا كآلية لكسر الـ heuristic.
  - *على محور N/¬N:* يوجد ارتباط تام في البيانات (75/75). **فرضية أنه اختصار مُتعلَّم رُفضت تجريبيًا في Phase 5 — انظر D40 المُعدَّلة.**
- **حدّ على أداة القياس نفسها (مرصود في D41):** الزوج P43 (gold=entailment، إعادة صياغة شبه حرفية للـ premise) سُجِّل `overlap=False`. ⇒ مقياس "5 tokens متتالية" **مؤشر ضعيف** على الاشتراك الدلالي في العربية بسبب الاختلاف الصرفي وترتيب الكلمات. الرقم 65 يقيس **تطابقًا سطحيًا**، لا تشابهًا دلاليًا.
- في `validate_data`: فحص الـ overlap مصنَّف **documented exception يُطبع في التقرير**، لا hard failure.

## D36 — استراتيجية الـ Validation Split (✅ مُصدَّق — 9 يوليو 2026)
- `val_fraction: 0.2` (عيّنة عشوائية) **مرفوض**: يخالف D26 لأنه قد يفصل الـ HARD_POS twins عبر حدود train/val، ويخالف قاعدة الـ Question-ID-level splitting.
- المعتمد: **document-level holdout على GN-008** (15 زوجًا من 150 = 10%)، مسجّل في `configs/nli_finetune.yaml` كـ `val_question_ids: ["GN-008"]`.
- المبرر: التقسيم على مستوى السؤال يضمن بنيويًا عدم انفصال الـ twins (كلاهما من نفس الـ question_id)، ويمنع تسرب المعرفة اللفظية بين train وval.
- **حد معروف (limitation):** val صغيرة (15 زوجًا) ⇒ تقديرات F1 عليها عالية التباين. دورها إيقاف التدريب/اختيار الـ checkpoint فقط. **الحكم النهائي يُبنى على الـ Gold Set (48 زوجًا، DS-014) في Phase 5**، لا على val.
- GN-008 مستبعد من الـ training pool بحكم كونه holdout — يُفحص آليًا مع D28.

## D37 — Data Staging على Kaggle (✅ — 9 يوليو 2026)
- ملفات الداتا مستبعدة من Git (`.gitignore`) بقرار §3، فالـ `git clone` لا يجلبها.
- الحل: خلية staging في الـ runner تكتشف الـ Kaggle Dataset (`iq-nli-finetune-data`) عبر بحث تكراري عن `reference_docs_250_FINAL_v1.json`، ثم تنسخ الملفات الأربعة إلى مسارات `data/` المتوقعة داخل الريبو المستنسخ. الاكتشاف بالمحتوى لا بالاسم — تحصينًا ضد اختلاف تسمية الـ dataset أو بنية التركيب.
- **استثناء موصوف لقاعدة "صفر منطق في النوتبوك" (§5.1):** هذا **منطق بيئة (environment staging)** لا منطق مشروع؛ لا يحتوي أي قرار خوارزمي، وحذفه لا يغيّر أي نتيجة.
- الـ Gold Set (`gold_set_48.json`) في Kaggle Dataset **منفصل** (`iq-gold-set`) ولا يُركَّب أبدًا في جلسة تدريب. الخلية تتضمن `assert` صريحًا بغيابه — **تعزيز لـ D28 على مستوى نظام الملفات**، لا على مستوى الكود فقط.
- *امتداد (Phase 5 — مُنفَّذ):* runner التقييم يطبّق الصورة المرآتية — يركّب `gold_set_48.json` ويؤكّد **غياب** ملفَّي أزواج الـ pilot، ويكتشف الـ adapter بالمحتوى (`adapter_config.json`) مع `RuntimeError` صريح عند عدم العثور عليه (لا fallback صامت). ويتضمن حارس `SILENT ADAPTER FAILURE SUSPECTED`: خروج بخطأ إذا تطابقت تنبؤات الذراعين على الـ 48 زوجًا كلها. *(الحارس اشتغل وأعطى OK في تشغيل D41 ⇒ الذراعان مختلفتان فعلًا، والنتيجة ليست artifact تحميل.)*

## D38 — نتيجة تشغيل Fine-tuning الأول (✅ مسجّلة — 9 يوليو 2026، RISK ACCEPTED)
- التشغيل على 135 زوج تدريب / 15 val (GN-008 holdout، D36). Kaggle T4 ×2.
- 5 epochs، 45 خطوة، ~6 دقائق. eval_loss: 0.789 → 0.482 → 0.344 → 0.309 → 0.303 (تناقص مطّرد ⇒ تقارب).
- best macro-F1 على val = 0.927 (epoch 3). **لا يُستشهد به كأداء الموديل:** val = 15 زوجًا، والفرق بين 0.861 و0.927 ≈ عيّنة واحدة (تباين عالٍ — D36).
- checkpoint (LoRA adapter + tokenizer) منشور كـ Kaggle Dataset `iq-checkpoints-nli-v1`.
- *تحقق (9 يوليو 2026):* `load_best_model_at_end` مفعّل — الـ adapter في جذر الـ checkpoint مطابق بايت-ببايت لـ `checkpoint-27` (epoch 3، أفضل macro-F1)، ومختلف عن `checkpoint-45` (الأخير). النسخة المنشورة نُظّفت: حالات الـ optimizer والـ checkpoints الوسيطة محذوفة (غير لازمة للـ inference).
- **نطاق الـ fine-tuning (مُصحَّح — تحقق تجريبي 9 يوليو 2026):** LoRA (r=16، α=32) على `query_proj`/`value_proj`، **بالإضافة إلى الـ classification head**. السبب: `peft.get_peft_model(..., task_type=SEQ_CLS)` يحقن تلقائيًا `modules_to_save = {'classifier','score'}` على مستوى `PeftModelForSequenceClassification`، حتى مع `LoraConfig.modules_to_save = None` في الـ YAML. أُثبت بتشغيل `wrap_with_lora()` على موديل DebertaV2 مصغّر: الرأس ملفوف في `ModulesToSaveWrapper` وبارامتراته trainable. **الرأس مدرَّب ومحفوظ داخل الـ adapter، وليس مجمَّدًا.** أي وصف سابق بخلاف ذلك مُلغى، ولا يُكتب في الرسالة.
- **V1 — تحقق سلامة الـ checkpoint (✅ مُغلق — 9 يوليو 2026):** فحص `adapter_model.safetensors` على Kaggle **قبل** أي تقييم أظهر **50 مفتاحًا**: 12 layer × 2 module (q/v) × 2 مصفوفة (lora_A/lora_B) = 48، **زائد** `base_model.model.classifier.weight` و`base_model.model.classifier.bias`. ⇒ الرأس المدرَّب نجا من عملية التنظيف، وكل الطبقات الـ12 معدَّلة. الفحص مُدمج كأول خلية في `run-nli-eval.ipynb` ويرفع `RuntimeError` عند غياب مفاتيح الـ classifier.
- **نتيجة اختبار Subject Blindness:** انظر **D41**. الـ baseline المعتمد = zero-shot على نفس الـ 48 زوج وبنفس code path (D39). **Demo #1 (D24) سياق كيفي فقط، وليس baseline رقميًا.**
- الوسم المنهجي: **RISK ACCEPTED (D33)**.

## D39 — قاعدة قرار مُسجَّلة مسبقًا لـ Phase 5 (✅ — سُجِّلت قبل التشغيل، commit `f4fbf27`)
- **الـ Gold Set:** DS-014، n=48 — E=17 · N=12 · C=19. (لا `paired_neutral`، لا labels خارج الثلاثة.)
- **توقيع الـ Subject Blindness = خلية C→E** (claim متناقض يُصنَّف entailment).

**معايير "كُسر الـ Subject Blindness" (الثلاثة مجتمعة):**
1. `C→E ≤ 2` من 19
2. `Contradiction recall ≥ 16/19`
3. `E→C ≤ 2` من 17 (ضمانًا ألا يكون العلاج انزياحًا أعمى ناحية C)

**قيود التفسير (مسجّلة مسبقًا):**
- الزوج الواحد = 5.3% من فئة C ⇒ العتبات تُكتب بالعدد لا بالنسبة. أي فرق أقل من 3 أزواج داخل نطاق الضوضاء.
- الحكم **اتجاهي (directional) لا إحصائي**. لا تُحسب دلالة معنوية على n=19.
- الـ baseline = zero-shot على نفس الـ 48 زوج بنفس code path (نفس `gold_eval.py`، بعلم `--zero-shot`).
- الـ Gold Set من **مستند واحد** ⇒ دليل على **وجود** العلاج، لا على **تعميمه** عبر المسارات الخمسة.
- الحكم يصدر بهذه المعايير حصريًا. macro-F1 مؤشر وصفي، لا معيار قبول.

**حدّ مُعترَف به بعد التنفيذ (D41):** القاعدة قيّدت الانزياح ناحية `contradiction` (المعيار 3) و**لم تقيّد الانزياح ناحية `neutral`** — وهو الانزياح الذي وقع فعلًا. **قاعدة قبول ناقصة.** الدرس المنهجي: معيار القبول يجب أن يقيّد **كل** أعمدة الـ confusion matrix، لا العمود المتوقَّع وحده.

## D40 — الـ overlap خاصية بيانات، لا اختصار مُتعلَّم (✅ مُعدَّل جوهريًا — 9 يوليو 2026)

**الوصف الإحصائي (قائم، لم يتغير):**
- pilot (150 زوجًا): 65 زوجًا بـ overlap ≥5 tokens — C=37 · E=28 · **N=0**.
- gold (48 زوجًا): 10 أزواج — C=5 · E=5 · **N=0**.
- ⇒ في المجموعتين معًا (75/75)، وجود الـ overlap يتنبأ تنبؤًا تامًا بأن الزوج ليس `neutral`.
- **السبب البنيوي:** أزواج الـ neutral (`paired_neutral` بحكم D26، و`standard_neutral` بحكم D32) تستمد الـ premise من chunk مختلف عن مصدر الـ hypothesis، فالتداخل اللفظي منخفض بطبيعته.

**اختبار تجريبي (Phase 5) — يرفض فرضية الاختصار المُتعلَّم:**

| الذراع (على أزواج overlap=True، n=10) | → entailment | → neutral | → contradiction |
|---|---|---|---|
| zero-shot | 6 | **0** | 4 |
| adapter | 4 | **3** | 3 |

- الموديل **الأساس** — لم يرَ بيانات التدريب إطلاقًا — تنبأ بـ `neutral` في **0/10**. **يستحيل أن يكون تعلّم artifact من ملف لم يره** ⇒ الارتباط `overlap ⇒ ¬neutral` **خاصية عامة في نماذج NLI المدرَّبة على MNLI/XNLI**، ومبرَّرة دلاليًا: علاقتا E وC تستلزمان اشتراكًا موضوعيًا بين premise وhypothesis، بينما N لا يستلزمه.
- الموديل **المدرَّب** تنبأ بـ `neutral` في **3/10** ⇒ **ابتعد** عن الارتباط بدل أن يتبنّاه.
- الأخطاء الستة الموجَّهة نحو `neutral` في ذراع الـ adapter موزَّعة **3 overlap / 3 no-overlap** ⇒ **لا إشارة**.

**الاستنتاج السابق ("الموديل قد يتعلم اختصارًا معجميًا على محور N/¬N") مسحوب — غير مدعوم، والدليل المتاح يعاكسه.**

**حدود هذا النفي:** n=10 أزواج overlap فقط في الـ Gold Set ⇒ الاختبار **ضعيف القوة (low power)**، والنفي ليس إثباتًا للعدم. يُذكر في الرسالة كـ "اختبار أُجري ولم يجد إشارة"، لا كـ "الاختصار غير موجود".

**الحالة:** الـ artifact في البيانات مُعلَن كـ limitation **وصفية**. لم يعد يُصنَّف تهديدًا لصلاحية النتائج.

## D41 — نتيجة Phase 5: Gold Set Evaluation (✅ — 9 يوليو 2026، RISK ACCEPTED)

**الحكم مقابل D39 (المسجَّلة مسبقًا في commit `f4fbf27`، قبل التشغيل): PASSED — الثلاثة مجتمعة.**

| المعيار | zero-shot | adapter | الحكم |
|---|---|---|---|
| C→E ≤ 2/19 | 3 | **0** | ✅ |
| Contradiction recall ≥ 16/19 | 16/19 | **16/19** | ✅ |
| E→C ≤ 2/17 | 1 | **0** | ✅ |

**خلايا E↔C أُفرغت بالكامل. الـ Subject Blindness — بالتعريف المسجَّل مسبقًا — مكسور.**

**قيود على قوة الاستدلال (منقولة من D39):**
- فرق C→E = 3 أزواج بالضبط، **عند الحد الأدنى** المسجَّل للدلالة الاتجاهية. فرق E→C = زوج واحد ⇒ **داخل نطاق الضوضاء، لا يُستشهد به منفردًا**.
- الحكم اتجاهي لا إحصائي (n=19). مستند واحد (DS-014) ⇒ دليل **وجود** العلاج لا **تعميمه**.

**ما لم يتحسّن — يُذكر صراحةً:** `Contradiction recall = 16/19` في الذراعين. ليست مصادفة بل **مقاصة**:
- **اتصلّح:** P12، P34 (كانا C→E في الـ zero-shot).
- **اتكسر:** P27، P31 (كانا صحيحين، صارا `neutral`).
- ⇒ **الموديل لم يكتشف تناقضات أكثر — أعاد توزيع أخطائه.**

**التكلفة تحت الـ argmax:** macro-F1: 0.880 → 0.850 · neutral precision: 0.917 → 0.647 · إجمالي الأخطاء: 6 → 7.

**المكسب تحت قاعدة الـ scoring (D21 + v2 Entailment-Priority):**
- `entailment precision = 1.000` ⇒ **صفر تأكيد كاذب** (لا claim خاطئ يُصنَّف VERIFIED).
- الخلايا المولِّدة لتأكيد كاذب أو عقوبة كاذبة (`C→E`, `E→C`) فرغت؛ ستة من سبعة أخطاء متبقية اتجاهها `neutral` (تُتجاهَل، α=0).
- ترتيب "قال صح > سكت > قال غلط" محفوظ.
- ⚠️ **هذا تحليل بعدي (post-hoc)، وليس معيار قبول.** لم يُسجَّل في D39، ويُكتب في الرسالة تحت *تحليل النتائج* لا تحت *معايير النجاح*.

**التفسير الميكانيكي لانزياح الـ neutral (مبني على D26 المكتوبة قبل التشغيل، لا على النتيجة):**
HARD_POS twins = 60/150 = **40% من إشارة التدريب**، تُعلّم صراحةً: نفس الـ hypothesis + premise عن كيان مختلف ⇒ `neutral`. الأثر المرصود (E→N: 1→3 · neutral precision ↓ · entailment precision → 1.000) هو النتيجة المتوقَّعة من هذا التصميم. **التصميم يعمل كما قُصد؛ الأثر الجانبي لم يكن مسجَّلًا.**

**حالات مسمّاة:**
- **P48** — gold=contradiction · zero-shot=E · adapter=**N**. **D29 (مكتوبة قبل التشغيل) تصف P48 نصًا بأنه weak contradiction: "طريقتا كشف غير متعارضتين".** تصنيف الموديل يتسق مع ذلك التوصيف، ومصداقية الليبل الذهبي محل شك. **الليبل لم يُعدَّل** — تعديله بعد رؤية النتيجة يُبطل الـ pre-registration. يُبلَّغ كـ **حدٍّ في صحة الـ Gold Set**، لا كتصحيح. أي إعادة تصنيف مستقبلية تستلزم إعادة تشغيل كاملة وتقرير النتيجتين.
- **P26** — gold=neutral · predicted=**contradiction في الذراعين**. الليبل صحيح (الـ premise عن L2 وساكت عن Elastic Net)، والموديلان مخطئان. **الخطأ الوحيد المتبقي المولِّد لعقوبة كاذبة** في محرك الـ scoring.
- **P43** — gold=entailment، إعادة صياغة شبه حرفية للـ premise، والذراعان قالا `neutral` رغم `overlap=False` ⇒ حدّ على مقياس الـ overlap نفسه (انظر D35).

**بند مؤجَّل إلى Phase 6 (⛔ — انظر V2):** التقييم كله `argmax` بلا عتبات، بينما محرك الـ scoring يستخدم **الاحتمالات الخام** و`τ_E = 0.9`. ملفا `gold_eval_*.json` **لا يحفظان أي softmax أو logits**. الأزواج P27/P31/P48 (المصنَّفة C→N) **قد تتجاوز `τ_E` وتُصنَّف VERIFIED** في المسار الحقيقي. **الـ argmax يخفي هذا الاحتمال تمامًا.**

**مخرجات محفوظة:** `gold_eval_zero_shot.json` · `gold_eval_adapter.json` (48 تنبؤًا per-pair لكل ذراع، بلا احتمالات).
**الوسم:** RISK ACCEPTED (D33).

---

## القسم الثالث — بنود مفتوحة تُرقَّم فور حسمها

| البند | الوصف | الحالة |
|---|---|---|
| **Q1** | توحيد ترقيم القرارات — السجل الموحّد D1–D41 هو هذا الملف. | ✅ |
| **Q4** | stage2_verdict الناقص: `validate_data` أكد وجوده على 15/15 من `pairs_DA001_pilot_v1.json`. فجوة توثيق لا فجوة منهجية. | ✅ |
| **Q5 → D35** | قبول مخالفة الـ 5-word overlap مع التوثيق. صُدِّق ومُصحَّح رقميًا. | ✅ |
| **Q9** | مستودع GitHub — https://github.com/HashemIlI/interview-iq (private). | ✅ |
| **V1** | تحقق سلامة الـ checkpoint (مفاتيح `classifier.*`) — نُفِّذ ونجح (50 tensor). انظر D38. | ✅ |
| **V2 — الاحتمالات الخام** | `gold_eval_*.json` لا تحفظ softmax/logits. مطلوب إضافتها لمخرجات التقييم **قبل** أي معايرة لـ `τ_E`. **تحجب أي حكم على سلوك الـ scoring** (D41). | ⛔ **تحجب Phase 6** |
| **Q8 — Coverage Asymmetry** | التأجيل انتهى بانتهاء Phase 5 (D34). **مفتوحة للنقاش الآن** كبند مستقل. | ⛔ (نشطة) |
| **Q2 — توثيق مراجعة الـ 250 مستندًا** | metadata "AI-generated, pending expert review" مُبقاة عمدًا لحين اعتماد المشرف. **مطلوب تثبيته كقرار مرقّم بمعيار المراجعة وهوية المراجع قبل التسليم الأكاديمي.** | ⛔ |
| **Q6 — AraT5 vs mT5-base** | اختيار موديل الـ decomposition لم يُحسم. مؤجل طبيعيًا لبوابة G2. | ⛔ |
| **Q7 / O11 — تطبيق التسجيل** | Tech Stack + Session Spec v1.0 + اعتماد الفريق. **يحجب G3** وبالتالي حسم الـ ASR في Phase 7. | ⛔ |
| **Q10 — عرض Demo #1** | هل عُرض الـ Baseline Demo (D24) على المشرف؟ deliverable غير مؤكد. | ⛔ |
| **O9 / بوابة G2** | تمارين الـ decomposition اليدوية + جلسة دليل التأليف — تحجب Phase 8. | ⛔ |
| **G3** | تسجيل الـ pilot videos — يحجب حسم اختيار الـ ASR في Phase 7 (مرتبط بـ Q7). | ⛔ |
| **O1** | فقرة تبرير Option B — تحجب التقرير النهائي. | ⛔ |

---

## سجل التغييرات
- **v2.3 (9 يوليو 2026):** إضافة **D41** (نتيجة Phase 5: حكم D39 = PASSED؛ مقاصة الـ contradiction recall؛ انزياح الـ neutral وتفسيره الميكانيكي من D26؛ الحالات P48/P26/P43). **إعادة كتابة D40 جوهريًا:** الاختبار التجريبي رفض فرضية الاختصار المُتعلَّم — الموديل الأساس أعطى 0/10 neutral على أزواج الـ overlap رغم أنه لم يرَ البيانات ⇒ الارتباط خاصية عامة في نماذج NLI، والاستنتاج السابق مسحوب. **إغلاق V1** (مفاتيح `classifier.*` مؤكَّدة، 50 tensor) وتحديث D38 بنتيجته. إضافة الحدّ المُعترَف به في D39 (لم تقيّد الانزياح ناحية N). تدقيق D25 بشأن مُفعِّل الـ fine-tuning الفعلي. تحديث D26/D29/D34/D35/D37 بالإحالات التجريبية. **فتح Q8** (Coverage Asymmetry) وإضافة **V2** (الاحتمالات الخام — تحجب Phase 6).
- **v2.2 (9 يوليو 2026):** إضافة D39 وD40. تصحيح D38 (نطاق الـ fine-tuning يشمل الـ classification head؛ الـ baseline = zero-shot على نفس الـ 48 زوج). إعادة كتابة D35 (حذف الرواية السببية غير المُتحقَّق منها وعتبة الـ 75% غير المسجَّلة؛ إضافة تعريف القياس ووسم lower bound). توسيع D31 إلى قاعدة عامة. إضافة امتداد Phase 5 إلى D37. إعادة إدراج Q6/Q7/Q10.
- **v2.1 (9 يوليو 2026):** إضافة D37 وD38. تعديل D33: إغلاق G1 بقبول المخاطرة مع نص الـ limitation الملزم. Q4 مُغلقة.
- **v2.0 (9 يوليو 2026):** دمج decisions.md القديم مع DECISIONS_RECONCILIATION_v1.1 في سجل موحّد. Q1 مُغلقة.
- **v1.x:** سجل جلسة الـ pipeline (D21–D26 بالترقيم القديم) — مؤرشف ضمنيًا في القسم الثاني أعلاه.
